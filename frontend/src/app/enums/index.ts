export enum EntityKind {
  'non défini' = 1,
  'école' = 2,
  'entrerpise' = 3,
}

export enum ProjectKind {
  'création de site' = 1,
  'desing d\'interface' = 2,
  'charte graphique' = 3,
  'stratégie de communication' = 4,
  'stratégie digitale' = 5,
}
